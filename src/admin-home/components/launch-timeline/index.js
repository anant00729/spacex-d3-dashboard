import React from "react";
import { LaunchTimelineWrapper } from "./styles";

function LaunchTimeline() {
  return (
    <LaunchTimelineWrapper>Here is the LaunchTimeline</LaunchTimelineWrapper>
  );
}

export default LaunchTimeline;
